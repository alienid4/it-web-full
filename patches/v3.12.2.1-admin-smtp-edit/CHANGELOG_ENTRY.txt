3.12.2.1 - 2026-04-27: SMTP 設定全欄位可編輯 + 密碼分離管理 - (1) admin#settings Email 區塊從原本只能改「收件人/觸發條件」擴成 6+2 欄位全部可編輯: SMTP 主機/Port/STARTTLS(下拉)/寄件帳號/From + 收件人/觸發條件; 密碼欄走獨立按鈕「更改」+「清除 (no auth)」, 主表單儲存時不送密碼以免占位符 round-trip 漏洩 (2) 後端 admin_settings GET 自動 mask plaintext smtp_pass 為 *** SET *** 不外傳 (ENV:VAR_NAME 形式因為只是變數名所以保留可見), admin_update_setting PUT notify_email 時若沒帶 smtp_pass 或收到 *** SET *** 占位符就 preserve 原值 (3) 新 POST /api/admin/settings/notify_email/password 專用端點: 接 password 字串, 空字串 = 清除走 no-auth (relay 用), 非空 = 直接寫入 settings.json (chmod 600 sysinfra:itagent 保護) 或寫 ENV:VAR_NAME 形式; 寫進 audit log (4) 新 POST /api/admin/settings/notify_email/test 寄一封 HTML 測試信給當前登入者 user.email (帳號沒設 email 會回 400) — v3.12.2.0 已加則 skip (5) 磁碟排除清單 (mounts + prefixes) chip 可編輯 — v3.12.2.0 已做則 skip (6) install.sh auto-detect 起點 (從原始 v3.11.x 直升 OR 從 v3.12.2.0 升級), 全部 idempotent (有 /* v3.12.2.1 */ marker 即 skip), Python regex surgical injection (admin.js 883 行 + api_admin.py 2952 行不適合整檔替換, 已對 221 真實檔做 dry-run 全 match + AST 語法 OK). 適用情境: 走 SMTP relay 不需 AUTH (port 25, no TLS, smtp_user 留空, 密碼按「清除」), 例 alien.lee 的 cathaysec relay.

3.12.4.0 - 2026-04-27: admin#backups 4 顆 dead button 灰掉 + Phase 2 開發中提示 - 用戶 console 看到「createDbDump is not defined」報錯, 確認 admin.html 有 button onclick 但 admin.js 從來沒定義對應 JS 函式 (createDbDump/importDbDump/uploadPatch/rollbackPatch/applyPatch 全沒實作), 是預先存在的 dead button bug (v2.1.0.0 寫過但被 sanitize 移除). v3.13.0.0 會補完, 此 patch 先 hot-fix 灰掉避免誤點: 5 顆按鈕加 disabled + opacity:0.5 + cursor:not-allowed + title="Phase 2 開發中 (預計 v3.13.0.0 補上)" tooltip + data-v3.12.4.0 marker (idempotent 用); dbbackup-list / patch-history「載入中...」(永遠不會完成) 換成「📅 Phase 2 開發中 — 預計 v3.13.0.0 上線」灰色置中 placeholder. 純 admin.html 修改, 不動 admin.js / 後端 / 任何邏輯. 「立即備份」(createBackup) 維持可用 (這個有實作). 套完強制清瀏覽器 cache.

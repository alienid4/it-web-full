3.12.2.0 - 2026-04-27: Admin 設定頁強化 (SMTP 測試 + 排除清單可編輯) - (1) admin#settings「磁碟排除清單」改為 chip 可編輯模式: mounts (完全比對) + prefixes (前綴比對) 各自獨立 add/remove input + 按鈕, 對齊 service_check_list 的既有 UX, 不再只是顯示 (2) admin#settings「Email 通知設定」儲存按鈕旁加「📨 測試 SMTP」按鈕, 點下去 POST /api/admin/settings/notify_email/test 後端用 services.email_service.send_email 寄一封測試信給當前登入者 user.email (HTML 格式含時間/觸發者/收件人/來源說明), toast 回報成功/失敗, 失敗時顯示具體 Exception 訊息 (e.g. SMTPAuthenticationError) (3) 新 route 走 @admin_required + log_action 記到 audit log, 帳號沒設 email 會回 400 提示先到「我的帳號」設好 (4) install.sh 走 surgical Python regex 注入 (不取代整檔), idempotent (有 v3.12.2.0 marker 就 skip), 失敗自動還原指引留 backup. 不需重新部署任何套件, 純前後端改動.

# 2026-04-23 09:30 — CPU/Memory 仍顯示 N/A 診斷

## 狀況

`inspection_*.json` 已產出 + 匯入，但 Dashboard 顯示 CPU / Memory N/A%，只有 Load Average（0.00/0.00/0.00）和 Swap/Users 有值。

## 要查：JSON 裡 `results.cpu` 到底有沒有數據

### 指令 1 — 看最新 inspection JSON 實際內容

```bash
sudo ls -t /opt/inspection/data/reports/inspection_*_SECSVR198-011T.json | head -1 | xargs sudo cat | python3 -m json.tool
```

**重點看**：
- `results.cpu` 是 `{}` 空 object？還是有 `cpu_percent`、`mem_percent` 欄位但值是 0 或空？
- `results.system` 有沒有 load / uptime？
- `results.disk` 有沒有資料？

### 指令 2 — 看 Dashboard API 回傳

```
http://10.92.198.13:5000/api/inspections/SECSVR198-011T/latest
```

看 `data.results.cpu.cpu_percent` 是什麼值。

### 指令 3 — 驗證 11 的 vmstat 能不能跑

```bash
# 在 13 上
sudo -u sysinfra ssh sysinfra@10.92.198.11 "vmstat 1 3 | tail -1 | awk '{print 100 - \$15}'"
```

**預期**：印出一個 0~100 的數字（CPU 使用率）。

如果印空白或錯誤 → `vmstat` 沒裝（需 `dnf install procps-ng`）或 sysinfra 沒權限跑。

---

## 判讀表

| JSON.results.cpu | vmstat ssh 結果 | 結論 | 下一步 |
|---|---|---|---|
| `{}` | 印數字 | check_cpu role 沒跑成 / set_fact 失敗 | 看 run log 找 check_cpu 的 TASK 是否 FAIL |
| `{cpu_percent:"0"}` | 印空白 | 11 的 vmstat 沒裝 / 權限不足 | `sudo dnf install -y procps-ng` on 11 |
| `{cpu_percent:50}` | 印數字 | 後端 OK，**Dashboard 前端讀錯欄位** | 看 dashboard.js 讀什麼 path |
| `{cpu_percent:"50"}`（字串非數字）| 印數字 | Dashboard 期待 number 但收到 string | 前端要 parseInt |

---

## 回傳格式

三段：

1. **指令 1** 的 JSON 輸出（特別是 `results` 整段）
2. **指令 2** 的 API 回傳 JSON
3. **指令 3** 的 vmstat 輸出

貼給我就能定位是後端沒資料、還是前端讀錯。

# 2026-04-23 16:00 — 現在要做的事（TODO NOW）

## 你現在 13 上是哪個版本？

先跑這條看 UI 顯示 + DB 記錄：

```bash
cat /opt/inspection/data/version.json | python3 -c 'import sys,json; d=json.load(sys.stdin); print("version:", d["version"])'
```

如果 < 3.11.19.0 表示下面這些還沒套。

---

## 優先序 3 顆（全部 1 檔 cp 搞定，每顆 1~2 分鐘）

### ① v3.11.17.0 seed 漏匯 account_audit（**最重要，不套 /audit 永遠空**）

```bash
TS=$(date +%Y%m%d_%H%M)
curl -fL -o /tmp/seed_data.py \
  https://github.com/alienid4/it-web-full/raw/main/AI/webapp/seed_data.py
grep -c "account_audit: " /tmp/seed_data.py   # 必須 >= 1

sudo cp /opt/inspection/webapp/seed_data.py /opt/inspection/webapp/seed_data.py.bak.${TS}
sudo cp /tmp/seed_data.py /opt/inspection/webapp/seed_data.py
sudo chown sysinfra:itagent /opt/inspection/webapp/seed_data.py

# 重跑 seed (reports/ 已有 JSON, 不必重跑 ansible)
cd /opt/inspection/webapp && sudo -u sysinfra python3 seed_data.py
```

預期看到 `account_audit: M 筆 (累計)` 且 M > 0。刷 /audit 表格就滿。

### ② v3.11.18.0 onboard 一鍵腳本（**加第 3 台用到**）

```bash
curl -fL -o /tmp/onboard_new_host.sh \
  https://github.com/alienid4/it-web-full/raw/main/AI/scripts/onboard_new_host.sh

sudo cp /tmp/onboard_new_host.sh /opt/inspection/scripts/onboard_new_host.sh
sudo chown sysinfra:itagent /opt/inspection/scripts/onboard_new_host.sh
sudo chmod 750 /opt/inspection/scripts/onboard_new_host.sh

# 驗證: 不帶參數會印 Usage
sudo /opt/inspection/scripts/onboard_new_host.sh
```

### ③ v3.11.19.0 patch_apply.sh 治本備份問題

```bash
curl -fL -o /tmp/patch_apply.sh \
  https://github.com/alienid4/it-web-full/raw/main/AI/scripts/patch_apply.sh

# 只有在 13 有 /opt/inspection/scripts/patch_apply.sh 才需覆蓋
if [ -f /opt/inspection/scripts/patch_apply.sh ]; then
    TS=$(date +%Y%m%d_%H%M)
    sudo cp /opt/inspection/scripts/patch_apply.sh /opt/inspection/scripts/patch_apply.sh.bak.${TS}
    sudo cp /tmp/patch_apply.sh /opt/inspection/scripts/patch_apply.sh
    sudo chown sysinfra:itagent /opt/inspection/scripts/patch_apply.sh
    sudo chmod 750 /opt/inspection/scripts/patch_apply.sh
    echo "✅ patch_apply.sh 系統版本已更新"
else
    echo "ℹ 系統沒裝 patch_apply.sh, 跳過 (之後 tarball 自帶改好版)"
fi
```

---

## 驗證（都跑完後）

### V1. /audit 該有滿滿帳號 + root 紅底

```bash
mongosh inspection --quiet --eval '
  const r = db.account_audit.findOne({user:"root"});
  print(r ? "✅ root uid="+r.uid+" gid="+r.gid : "❌ 還是空");
  print("總筆數:", db.account_audit.countDocuments({}));
'
```

### V2. onboard 腳本存在可執行

```bash
ls -la /opt/inspection/scripts/onboard_new_host.sh
```

### V3. patch_apply.sh 新版（若有系統版本）

```bash
grep -c "ignore-failed-read" /opt/inspection/scripts/patch_apply.sh 2>/dev/null
# 應該 >= 1 (新版標記)
```

---

## 下一步（加第 3 台時）

① UI `/admin#hosts` 新增主機（填 hostname/IP/ssh_user/ssh_port/ssh_key）

② SSH 到 13 跑一條：
```bash
sudo /opt/inspection/scripts/onboard_new_host.sh <新hostname>
```

③ 等 3~7 分鐘，腳本印 6 個驗證 URL，逐個檢查。

---

## 回傳

跑完每一顆（①②③）的**最後一個指令輸出**：
1. ① seed_data.py 輸出（`account_audit: M 筆` 的 M 是多少）
2. ② onboard_new_host.sh 的 Usage 印出來
3. ③ 是否看到 `✅ patch_apply.sh 系統版本已更新`

三個都 OK 後告訴我，下面要不要做 Phase 3（主管簽核報表 /audit/privileged）。

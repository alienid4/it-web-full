# 2026-04-24 11:00 — v3.11.23.1 子項標籤彩色化

**改了什麼**：深度檢查展開細節裡的 `[PASS]` / `[WARN]` / `[FAIL]` / `[SKIP]` 文字變成**彩色小 badge**（綠 / 橘 / 紅 / 灰），讓子項異常一眼看到。

## 在 13 上套（1 分鐘，單檔 patch）

```bash
cd /tmp
rm -rf v3.11.23.1 patch_v3.11.23.1.tar.gz 2>/dev/null
curl -fLO https://github.com/alienid4/it-web-full/releases/download/v3.11.23.1/patch_v3.11.23.1.tar.gz
tar xzf patch_v3.11.23.1.tar.gz
cd v3.11.23.1
sudo bash install.sh
```

## 驗證

Ctrl+F5 `/report` → 跑深度檢查 → 展開 `[2/9] 頻寬` 等 WARN 項目 → 裡面的 `[WARN]` `[PASS]` `[SKIP]` 應該各自是橘/綠/灰小 badge。

## Rollback

```bash
BACKUP=$(ls -td /var/backups/inspection/pre_v3.11.23.1_* | head -1)
sudo cp -p $BACKUP/report.html /opt/inspection/webapp/templates/
sudo cp -p $BACKUP/version.json /opt/inspection/data/
sudo systemctl restart itagent-web
```

PREREQ: v3.11.23.0 已裝

# 2026-04-24 10:30 — v3.11.23.0 深度檢查 5 秒視覺化

**做了什麼**：深度檢查執行完後，modal 不再是整坨文字 — 改成 **大燈 + 4 KPI + 9 矩陣 + 可展開**。

**P0 範圍**：只做視覺化 + 180 天保留。Phase 1 (排程) / Phase 2 (告警) / Phase 3 (告警頁) 未來再出。

---

## 套用（2 分鐘）

### 在 13 上跑

```bash
cd /tmp
rm -rf v3.11.23.0 patch_v3.11.23.0.tar.gz 2>/dev/null
curl -fLO https://github.com/alienid4/it-web-full/releases/download/v3.11.23.0/patch_v3.11.23.0.tar.gz
tar xzf patch_v3.11.23.0.tar.gz
cd v3.11.23.0
sudo bash install.sh
```

install.sh 5 步：備份 → 複製新檔 → py 語法驗證 → **自動裝 cron** → 重啟 itagent-web → 驗證 API。

---

## 驗證

### 1. 裝 cron 成功？

```bash
cat /etc/cron.d/inspection-deep-check-cleanup
```

應看到：`0 3 * * * root find /opt/inspection/data/deep_check_reports -name 'ts_*_*.txt' -type f -mtime +180 -delete`

### 2. 版本對？

```bash
cat /opt/inspection/data/version.json | python3 -c 'import sys,json; print(json.load(sys.stdin)["version"])'
```

應回 `3.11.23.0`。

### 3. 瀏覽器視覺化

**Ctrl+F5** `/report` → 點任何 Linux 卡片「🔍 深度檢查」→ 按「▶ 開始執行」→ 等 30-60 秒。

**預期畫面**（完成後 modal）：

```
┌──────────────────────────────────────────────────┐
│ 🟡   警告 — 有可觀察項                           │
│      secclient1 · rhel · up 3 days · 22:09      │
├──────────────────────────────────────────────────┤
│   ✅ 6      🟡 2      🔴 0      ⚪ 1            │
│   正常     警告      異常      略              │
├──────────────────────────────────────────────────┤
│ 🟡 [2/9] 頻寬      NIC 累積錯誤 / retrans...  ▲ │
│    檢查範圍: ...                                │
│    正常基準: ...                                │
│    實測數值: ...                                │
│    🔧 建議動作: conntrack 高 → sysctl -w ...    │
├──────────────────────────────────────────────────┤
│ 🟡 [6/9] 時間/憑證 NTP 未同步會導致...       ▲ │
│    ...                                           │
├──────────────────────────────────────────────────┤
│ ✅ [1/9] 效能      Load 1.2 / 4core          ▼ │
│ ✅ [3/9] AP Port   (未偵測 / 非 AP 角色)     ▼ │
│ ...                                              │
├──────────────────────────────────────────────────┤
│ 💬 客訴「慢/連不進去」: 可能相關                 │
│                                                  │
│ [⬇ Summary] [📄 查看 raw summary] [⬇ Detail]    │
└──────────────────────────────────────────────────┘
```

重點觀察：
- WARN/FAIL 項目 **自動展開**（不用自己找）
- PASS 項目摺疊，點 ▼ 展開
- 底下「查看 raw summary」仍可看原文（給想看細節的老手）

---

## 回報

貼截圖（或說好 / 不好）給我，尤其想看：
- 視覺是否清楚？
- 9 矩陣的摘要欄夠用嗎？（目前 WARN 顯示 impact，PASS 顯示 range）
- 有什麼想調的 icon / 顏色 / 排版？

---

## Rollback

install.sh 末段會印完整指令。主要是：

```bash
BACKUP=$(ls -td /var/backups/inspection/pre_v3.11.23.0_* | head -1)
sudo cp -p $BACKUP/api_deep_check.py /opt/inspection/webapp/routes/
sudo cp -p $BACKUP/report.html /opt/inspection/webapp/templates/
sudo cp -p $BACKUP/version.json /opt/inspection/data/
sudo rm -f /etc/cron.d/inspection-deep-check-cleanup
sudo systemctl restart itagent-web
```

# 2026-04-24 09:20 — v3.11.22.0 在 13 上深度檢查失敗診斷

**症狀**：點深度檢查 → 彈 modal → 按「開始執行」→ 錯誤訊息「**網路錯誤：Unexpected token '<'**」
**判讀**：JS 期待 JSON，但 API 回了 HTML（404 錯誤頁 / 登入頁 / Flask 錯誤頁）

**目標**：判出是下列哪條
- A. blueprint 沒註冊（install.sh 的 sed 插入失敗）
- B. itagent-web 沒重啟（還跑舊 code）
- C. Flask 起不來 / 語法錯誤

---

## 在 13 上依序跑

```bash
echo "========== [1] 版本 =========="
cat /opt/inspection/data/version.json | python3 -c 'import sys,json; print(json.load(sys.stdin)["version"])'

echo ""
echo "========== [2] app.py 有沒有 deep_check =========="
grep -n "deep_check" /opt/inspection/webapp/app.py

echo ""
echo "========== [3] blueprint 檔案存在? =========="
ls -la /opt/inspection/webapp/routes/api_deep_check.py

echo ""
echo "========== [4] Flask service 狀態 + 重啟時間 =========="
systemctl status itagent-web --no-pager | head -10
echo "---"
systemctl show itagent-web --property=ActiveEnterTimestamp

echo ""
echo "========== [5] API 實際回什麼 =========="
curl -s -o /tmp/dc_test.out -w 'HTTP: %{http_code}\n' http://127.0.0.1:5000/api/deep-check/reports
echo "--- body 前 200 字元 ---"
head -c 200 /tmp/dc_test.out; echo

echo ""
echo "========== [6] Flask 啟動 log (近 50 行, 找 ImportError/SyntaxError) =========="
journalctl -u itagent-web -n 50 --no-pager | grep -E "ImportError|SyntaxError|deep_check|ERROR|Traceback" | tail -20
echo "---(最後 20 行原始 log)---"
journalctl -u itagent-web -n 20 --no-pager | tail -20

echo ""
echo "========== [7] smit_menu 是否就位 =========="
ls /opt/inspection/scripts/smit_menu/ 2>&1 | head -5
echo "總檔數: $(ls /opt/inspection/scripts/smit_menu/ 2>/dev/null | wc -l)"

echo ""
echo "========== [8] 看 install.sh 有沒有跑完 =========="
ls -la /var/backups/inspection/pre_v3.11.22.0_* 2>/dev/null | tail -3
```

---

## 判讀表

| [1] version | [2] grep deep_check | [3] api_deep_check.py | [5] HTTP | [6] journalctl | 結論 |
|---|---|---|---|---|---|
| 3.11.22.0 | 有 2 行 | 存在 | **401** | 乾淨 | ✅ 沒問題，失敗可能是**受監控主機 sudo**（看截圖 modal 裡「開始執行」後才錯？重跑一次看哪個 hostname）|
| 3.11.22.0 | 有 2 行 | 存在 | **404** | 乾淨 | ❌ **Flask 沒重啟 → `sudo systemctl restart itagent-web` 再測** |
| 3.11.22.0 | **0 行 / 只有 1 行** | 存在 | 404 | 乾淨 | ❌ **install.sh 的 sed 插入失敗**（13 的 app.py 錨點不一樣）→ 貼 [2] 出來我手動修 |
| 3.11.22.0 | 有 2 行 | 存在 | 500 | 有 ImportError/SyntaxError | ❌ Flask 起不來 → 貼 [6] 錯誤訊息 |
| 3.11.22.0 | 有 2 行 | **不存在** | 404 | 乾淨 | ❌ 複製失敗（權限問題）→ 貼 [3] 錯誤 |
| 3.11.21.0 | 0 行 | 不存在 | 404 | - | ❌ install.sh 根本沒跑成功 → 貼 install.sh 當時輸出 |

---

## 回貼給我

1. [1]~[8] 全部輸出（直接貼）

我一看就能判是哪條，然後要嘛一條指令修好、要嘛發 hot-fix。

---

## 若 [5] HTTP 是 401 的話（blueprint OK），重測深度檢查並看這個

```bash
# 重新登入 + 在 browser 按一次深度檢查
# 然後看 /tmp/flask-error OR journalctl 最新的錯誤
journalctl -u itagent-web --since "1 minute ago" | tail -30
```

這會抓到 ansible 跑失敗的原因（八成是受監控主機 sudo 問題，跟 onboarding 老帳有關）。

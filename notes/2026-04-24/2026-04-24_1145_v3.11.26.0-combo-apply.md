# 2026-04-24 11:45 — v3.11.26.0 Combo 一口氣 22.2 → 26.0

**為什麼有這個**：原本要跑 5 顆 patch 才到 26.0，現在打成**一顆 combo**，跑一次就升級完整。

## 涵蓋範圍

| 原 patch | 功能 |
|---|---|
| v3.11.23.0 | 視覺化 hero + 9 矩陣 + 4 KPI + WARN 自動展開 + **180 天保留 cron** |
| v3.11.23.1 | [PASS]/[WARN]/[FAIL]/[SKIP] 彩色 badge |
| v3.11.24.0 | 建議動作知識庫 (📋 指令 / ⚠️ 風險 / ✅ 驗證) |
| v3.11.25.0 | 歷史瀏覽（不用重跑）+ _detect_inspection_home bugfix |
| v3.11.26.0 | 版本對照（並列 9 面向比較） |

## 在 13 上跑

```bash
cd /tmp
rm -rf v3.11.26.0-combo patch_v3.11.26.0-combo.tar.gz 2>/dev/null
curl -fLO https://github.com/alienid4/it-web-full/releases/download/v3.11.26.0/patch_v3.11.26.0-combo.tar.gz
tar xzf patch_v3.11.26.0-combo.tar.gz
cd v3.11.26.0-combo
sudo bash install.sh
```

install.sh 跑 7 步，約 1 分鐘：
1. 前置檢查（版本、必要檔存在）
2. 備份到 `/var/backups/inspection/pre_v3.11.26.0_combo_<TS>/`
3. 複製 4 檔（`api_deep_check.py`、**新檔 `remedy_kb.py`**、`report.html`、`version.json`）
4. Python 語法驗證
5. 建 `deep_check_reports/` + `deep_check_progress/` 目錄
6. 裝 `/etc/cron.d/inspection-deep-check-cleanup`（每天 03:00 清 180 天前）
7. 重啟 itagent-web + 驗證 endpoint + blueprint import 測試

## 預期結果

```
版本: 3.11.26.0
endpoints: 7  remedies: 10
```

## 5 步瀏覽器驗證

Ctrl+Shift+R `/report` → 點任一 Linux 卡片「🔍 深度檢查」：

1. **歷史** — modal 上方「📋 最近跑過」列表（若有）
2. **視覺化** — 跑完後 hero 大燈 + 4 KPI + 9 矩陣
3. **彩色 badge** — 展開 WARN 項目，內部 `[PASS]` `[WARN]` 是彩色標籤
4. **知識庫** — WARN 底部藍框 📖 指令（可複製 📋）/ 風險 / 驗證
5. **對照** — 下方「📊 對照其他」→ 挑版本 → 並列對照表

## Rollback 全退

install.sh 末段會印完整指令。主要是從 `/var/backups/inspection/pre_v3.11.26.0_combo_<TS>/` 把 4 個檔還原 + 刪 `remedy_kb.py` + 刪 cron + 重啟。

## 跟 5 顆分版相比

| | 5 顆 分別套 | Combo |
|---|---|---|
| 下載次數 | 5 | 1 |
| 備份數 | 5 份分別 | 1 份統一 |
| 耗時 | ~10 分鐘 | ~1 分鐘 |
| rollback 顆粒 | 可退到每個中間版 | 全退到 22.2 |
| 適用 | 想細測各版效果 | 直接要最終版 |

你 22.2 → 26.0 就用 combo 最快。

# 2026-04-24 09:15 — v3.11.22.1 hot-fix 套用

**根因已鎖定**：`api_deep_check.py` 把 INSPECTION_HOME hardcode 成 `/seclog/AI/inspection`（221 路徑）。
13 用 `/opt/inspection`，makedirs 時試建 `/seclog/AI/...`，sysinfra 沒權限 → 500 HTML。

**修法**：改為 auto-detect（優先 env var，後備掃 /opt 和 /seclog）。

---

## 在 13 上套用（2 分鐘）

```bash
cd /tmp
rm -rf v3.11.22.1 patch_v3.11.22.1.tar.gz 2>/dev/null
curl -fLO https://github.com/alienid4/it-web-full/releases/download/v3.11.22.1/patch_v3.11.22.1.tar.gz
tar xzf patch_v3.11.22.1.tar.gz
cd v3.11.22.1
sudo bash install.sh
```

install.sh 做 4 步：備份 → 複製 api_deep_check.py + version.json → 重啟 itagent-web → 驗證。

---

## 驗證

1. 看 install.sh 末段有 `v3.11.22.1 hot-fix 安裝完成!`
2. 版本檢查：
   ```bash
   cat /opt/inspection/data/version.json | python3 -c 'import sys,json; print(json.load(sys.stdin)["version"])'
   ```
   應回 `3.11.22.1`

3. **瀏覽器 Ctrl+F5** `/report` → Linux 卡片點深度檢查 **(選 011T 或 014T)** → ▶ 開始執行

### 預期

進度條動 → 30–60 秒 → 出現 summary 預覽（PASS/WARN/FAIL 判定字卡）。

### 若還是失敗

- 重新貼 F12 Network → run → Response body 前 3 行
- 重新貼 `sudo journalctl -u itagent-web --since "1 minute ago"`

我再看新的 traceback。

---

## Rollback（若 install.sh 有錯）

install.sh 末段會印，大致這樣：
```bash
BACKUP=$(ls -td /var/backups/inspection/pre_v3.11.22.1_* | head -1)
sudo cp -p $BACKUP/api_deep_check.py /opt/inspection/webapp/routes/
sudo cp -p $BACKUP/version.json /opt/inspection/data/
sudo systemctl restart itagent-web
```

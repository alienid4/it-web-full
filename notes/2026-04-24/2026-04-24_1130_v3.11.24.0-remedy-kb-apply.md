# 2026-04-24 11:30 — v3.11.24.0 深度檢查建議動作知識庫

**做了什麼**：深度檢查展開 WARN/FAIL 項目時，建議動作下方多 **3 個實用子區塊**：

- 📋 **指令（可複製）** — 一鍵複製
- ⚠️ **風險** — 套用前風險提示
- ✅ **驗證** — 改完怎麼確認

## 在 13 上套

```bash
cd /tmp
rm -rf v3.11.24.0 patch_v3.11.24.0.tar.gz 2>/dev/null
curl -fLO https://github.com/alienid4/it-web-full/releases/download/v3.11.24.0/patch_v3.11.24.0.tar.gz
tar xzf patch_v3.11.24.0.tar.gz
cd v3.11.24.0
sudo bash install.sh
```

install.sh 會備份 → 複製 4 檔（含**新檔 `remedy_kb.py`**）→ 語法檢查 → 重啟 → 驗證。

## 測試

Ctrl+Shift+R `/report` → 跑深度檢查（任何 WARN 項目）→ 展開 `[2/9] 頻寬` 或 `[8/9] Infra` → 應看到：

```
🔧 建議動作：（既有）
conntrack 高 → sysctl -w ...

┌─ 📖 conntrack 接近滿 ─────────────────────────────┐
│ 📋 指令 (可複製)                                   │
│ # 暫時生效                                         │
│ [sudo sysctl -w net.netfilter.nf_conntrack_max=...] 📋│
│ ...                                                │
│                                                    │
│ ⚠️ 風險                                           │
│ • 記憶體: conntrack_max=262144 ≈ 75MB RAM         │
│ • ...                                              │
│                                                    │
│ ✅ 驗證                                            │
│ [cat /proc/sys/.../nf_conntrack_max]            📋 │
│ ...                                                │
└───────────────────────────────────────────────────┘
```

## 初始 10 條涵蓋

| 面向 | 情境 |
|---|---|
| 頻寬 | conntrack / TIME_WAIT / SYN drop / NIC 錯誤 (4) |
| 時間/憑證 | NTP / 憑證到期 (2) |
| Infra | OOM / failed units (2) |
| 效能 | Load 過高 (1) |
| Storage | 磁碟滿 (1) |

**可擴充**：`/opt/inspection/webapp/routes/remedy_kb.py` 是 Python dict，未來看到沒涵蓋的情境直接加到 `REMEDY_KB` list。

## 要改的話

知識庫內容（指令/風險）若需調整，告訴我改什麼，我直接改 remedy_kb.py 發小 patch。這種微調不需要大改 code。

## Rollback

install.sh 末段會印。主要是還原 3 個檔 + 刪新檔 remedy_kb.py + 重啟。

PREREQ: v3.11.23.1

# 2026-04-24 09:45 — 瀏覽器 POST 真實 error trace

**最新情報**：
- `_enforce_login` + `track_active_user` + `_check_feature_flag` 都不擋（`/api/deep-check/` 不在 _FEATURE_PATH_MAP）
- curl 無 cookie → POST 正常回 401 JSON
- **所以 browser（已登入）的 POST 是真的跑到 route handler 了，handler 內部爆了**

**嫌疑**：我的 `run_deep_check` 在 13 環境下遇到異常（MongoDB 沒找到主機、或 ansible 路徑問題、或寫檔權限），Flask 預設回 HTML 500（含 traceback）。

---

## 在 13 上跑（邊 browser 點按鈕邊看 log）

### 步驟 1：清 log 起始點

```bash
# 記住現在時間作為 log 起點
NOW=$(date '+%H:%M:%S')
echo "起點時間: $NOW"
```

### 步驟 2：到 browser 點「🔍 深度檢查」→ 選任一非 013 主機（例如 014T） → 按「▶ 開始執行」

等錯誤訊息出現後回 terminal 繼續。

### 步驟 3：抓完整 traceback

```bash
sudo journalctl -u itagent-web --since "1 minute ago" --no-pager
```

**重點看**：
- `POST /api/deep-check/run HTTP/1.1" <STATUS>` 那行的 `<STATUS>` 是幾？（200 / 400 / 500 / 302？）
- 如果 500，前面會有 `Traceback (most recent call last):` 幾行 Python 錯誤
- 如果 302，會有 redirect 目標

### 步驟 4：用 F12 直接看 response（最精準）

browser 按 F12 → Network tab → 再點一次深度檢查按鈕 → 找那個 `run` 請求:

| 看什麼 | 告訴我 |
|---|---|
| **Status Code** | 200 / 400 / 500 / 302 ? |
| **Response Headers → Content-Type** | `application/json` 還是 `text/html`? |
| **Response tab → Response body 前 3 行** | 是 `{"success":...}` 還是 `<!DOCTYPE html...`? |

---

## 如果 traceback 是 MongoDB / collection 找不到

可能我的 api_deep_check 在查 `db.hosts.find_one({"hostname": ...})` 時， 13 的 DB schema 某欄位不同。貼 traceback 給我就能判。

## 如果 traceback 是 ansible 相關

可能 `ANSIBLE_DIR` / `VAULT_PASS` 路徑在 13 跟 221 不同：

```bash
# 13 上確認路徑存在
ls -la /opt/inspection/ansible/
ls -la /opt/inspection/.vault_pass
ls -la /opt/inspection/ansible/playbooks/deep_check.yml
ls -la /opt/inspection/ansible/inventory/hosts.yml
```

## 如果是 302 redirect

某個 middleware 把 POST 重導向 → 把 [E] 的 3 個 before_request **完整內容** 貼給我：

```bash
sed -n '14,30p;36,55p;140,170p' /opt/inspection/webapp/app.py
```

---

## 回貼給我

1. **F12 Network → `run` 請求的 Status Code + Content-Type + Response body 前 3 行**（最重要）
2. 步驟 3 的 journalctl 完整輸出（特別是 POST 那行附近）
3. 如果是 ansible 路徑問題，貼「額外診斷」的 ls 結果

這次一定找得到。

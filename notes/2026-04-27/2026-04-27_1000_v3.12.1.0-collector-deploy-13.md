# 2026-04-27 10:00 — v3.12.1.0 VMware collector 部署到 13

目的：把 VMware collector (接真 vCenter) 套到公司測試區 13。

**前置**：
- v3.12.0.0-vmware-prereq 已套（pyvmomi + python-ldap）
- v3.12.0.0-vmware-tab-w1 已套（VMware tab UI + mock fallback）
- 5 個 vCenter 從 13 連得到（板橋/內湖-1/內湖-2/VCF/敦南）
- 有 administrator@vsphere.local 帳密

**預期耗時**：套 patch 2 分鐘 + 設定 vault 5 分鐘 + dry-run 驗證 5 分鐘 = 12 分鐘

---

## 在 13 上依序跑

### 步驟 1：下載 + 解壓 patch（從 GitHub Release）

```bash
cd /tmp
rm -rf v3.12.1.0-vmware-collector patch_v3.12.1.0.tar.gz 2>/dev/null
curl -fLO https://github.com/alienid4/it-web-full/releases/download/v3.12.1.0-vmware-collector/patch_v3.12.1.0-vmware-collector.tar.gz
tar xzf patch_v3.12.1.0-vmware-collector.tar.gz
ls -la v3.12.1.0-vmware-collector/
cat v3.12.1.0-vmware-collector/patch_info.txt
```

預期看到 `install.sh` + `CHANGELOG_ENTRY.txt` + `patch_info.txt` + `files/` (5 檔)。

### 步驟 2：套 patch（install.sh 自動備份）

```bash
cd /tmp/v3.12.1.0-vmware-collector
sudo bash install.sh
```

install.sh 走 8 步：前置檢查（pyvmomi+ansible-vault+w1 prereq）→ 備份 → 部署 collector → 部署 service+改 api → 建 data/vmware/ → 安裝 cron → bump version.json → 重啟驗證。

### 步驟 3：基本驗證（先不接真 VC）

```bash
# [A] 版本
cat /opt/inspection/data/version.json | python3 -c 'import sys,json; print(json.load(sys.stdin)["version"])'
# 預期: 3.12.1.0

# [B] cron 已裝
cat /etc/cron.d/inspection-vmware-collect
# 預期: 0 */8 * * * sysinfra /usr/bin/python3 /opt/inspection/collector/...

# [C] collector 可執行
sudo -u sysinfra python3 /opt/inspection/collector/vcenter_collector.py --help

# [D] /vmware 頁面還能開（fallback 到 mock, 因為還沒填 vcenters.yaml）
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:5000/vmware
# 預期: 302 (未登入轉 login) 或 200

# [E] data/vmware/ 目錄已建
ls -la /opt/inspection/data/vmware/
# 預期: vcenters.yaml.sample + README.md (還沒有 vcenters.yaml)
```

### 步驟 4：填 vcenters.yaml + vc_credentials.vault

→ 接 `notes/2026-04-27_1015_v3.12.1.0-setup-vault-13.md`

### 步驟 5：第一次接真 VC dry-run

→ 接 `notes/2026-04-27_1030_v3.12.1.0-first-real-collect-13.md`

---

## Rollback（若任何步驟失敗）

```bash
sudo rm /etc/cron.d/inspection-vmware-collect
sudo rm -rf /opt/inspection/collector
sudo rm -rf /opt/inspection/data/vmware
sudo rm /opt/inspection/webapp/services/vmware_service.py

BACKUP=$(ls -td /var/backups/inspection/pre_v3.12.1.0-vmware-collector_* | head -1)
sudo cp -p $BACKUP/api_vmware.py /opt/inspection/webapp/routes/
sudo cp -p $BACKUP/version.json /opt/inspection/data/
sudo systemctl restart itagent-web
```

驗證 rollback：`cat /opt/inspection/data/version.json` 應回到 3.12.0.0。
